{
  "buildCommand": "cd white-tiger-video && npm install --legacy-peer-deps && npm run build",
  "outputDirectory": "white-tiger-video/build",
  "installCommand": "echo 'build handled in buildCommand'"
}
